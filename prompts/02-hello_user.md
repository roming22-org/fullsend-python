# Print the username instead of "World"

1. "World" is to be used as the default if the username can't be retrieved.
2. The new code should be covered by unit tests.
3. The documentation should be updated to reflect the latest changes
